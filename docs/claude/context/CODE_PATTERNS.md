# Code Patterns & Conventions

> Last updated: {{DATE}}

## Tech Stack

<!-- List your technology choices -->
<!-- Example:
- **Backend:** Python 3.12, FastAPI, uvicorn
- **Frontend:** SvelteKit, Tailwind CSS
- **Testing:** pytest (backend), svelte-check (frontend)
- **Formatting:** black (Python), prettier (JS/TS)
-->

## Naming Conventions

<!-- Example:
- Python: snake_case for functions/variables, PascalCase for classes
- TypeScript: camelCase for functions/variables, PascalCase for components
- Files: kebab-case for frontend, snake_case for backend
-->

## Common Patterns

<!-- Document recurring patterns in your codebase -->
<!-- Example:
### API Route Pattern
```python
@router.get("/items")
async def get_items(user=Depends(get_current_user)):
    service = ItemService(user.id)
    return service.list_all()
```
-->

## Error Handling

<!-- How errors are handled across the stack -->

## Testing Conventions

<!-- Test file naming, fixture patterns, what to mock -->
